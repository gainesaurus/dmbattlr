import React from 'react';

import styles from './TurnTracker.module.css';

const TurnTracker = (setIsTheirTurn: any) => {

  return (
    <section className={styles.container}>
    </section>
  )
}

export default TurnTracker;
